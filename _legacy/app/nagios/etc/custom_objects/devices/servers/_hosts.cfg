define host {
    use                     og-server
    host_name               localhost
    alias                   localhost
    address                 127.0.0.1
}

define host {
    use                     og-server
    host_name               esxi
    alias                   esxi
    address                 127.0.1.2
}

define host {
    use                     og-server
    host_name               docker_host
    alias                   docker_host
    address                 127.0.1.3
}

define host {
    use                     og-server
    host_name               ms_server
    alias                   ms_server
    address                 127.0.1.4
}